<html>
  <head>
    <meta name="viewport" content="initial-scale=1.0, width=device-width" />
    <meta http-equiv="X-UA-Compatible" content="IE=7; IE=EmulateIE9; IE=10"/>
    <script src="http://js.api.here.com/v3/3.0/mapsjs-core.js"
    type="text/javascript" charset="utf-8"></script>
    <meta http-equiv="X-UA-Compatible" content="IE=7; IE=EmulateIE9; IE=10"/>
    <script src="http://js.api.here.com/v3/3.0/mapsjs-service.js"
    type="text/javascript" charset="utf-8"></script>
  </head>
  <body>
    <div style="width: 640px; height: 480px" id="mapContainer"></div>
    <script>
      // Initialize the platform object:
      var platform = new H.service.Platform({
        'app_id': 'rIiShqffvZj8EyDeOq15',
        'app_code': 'Wnf__6rH5EoRKTr8WlHI7w'
      });
    <var> center_map = function get_midpoint(location1, location2) {
  y = (location1.lat + location2.lat) / 2;
  x = (location2.lng + location2.lng) / 2;
  return {lat: x, lng: y}
}
    
    </var> // Do we necessarily need to close this? I don't think so or know
      // Obtain the default map types from the platform object
      var maptypes = platform.createDefaultLayers();

      // Instantiate (and display) a map object:
      // Obtain the default map types from the platform object:
var defaultLayers = platform.createDefaultLayers();

// Instantiate (and display) a map object:
var map = new H.Map(
  document.getElementById('mapContainer'),
  defaultLayers.normal.map,
  {
    zoom: 10,
    center: { lat: 37.5, lng: 123.4 }
  });
  var searchCenter = {
	center_map 

  nokia.places.search.manager.findPlacesByCategory({
	category: 'eat-drink',"leisure-outdoor", "sights-museums",
	searchCenter: searchCenter,
	onComplete: function(data, status){
		// handle and/or process data here
		//Everything not completely working, on debugs now
	}
});
    </script>
  </body>
</html>
